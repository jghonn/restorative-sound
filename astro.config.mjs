// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update if a different final domain is chosen.
  site: 'https://restorativesoundhealing.com',
});
